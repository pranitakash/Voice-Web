---
trigger: always_on
---

STOP & PIVOT: > "You have built a marketing landing page. This is NOT the goal. The Goal: Build the Internal Builder Tool itself.

Immediate Fixes:

Rename: Change all branding from 'Giga.ai Builder' to 'Voice Web Studio'.

The Canvas: The main screen should NOT be a chat interface. It should be a Blank Workspace (the Canvas) where sections (Hero, Bento, etc.) actually appear when I speak.

Voice Logic Fix (Windows): Agent 2, check if you are using window.webkitSpeechRecognition. Ensure you have an onresult listener that specifically triggers the addSection function in our Zustand store.

Live UI: When I say 'Add Hero', a Hero component must instantly render onto the dot-grid canvas. It shouldn't just be a text reply in a chat."